{
  "name": "campusbridge-platform",
  "version": "1.0.0",
  "private": true,
  "description": "CampusBridge – University Alumni Network Engagement and Career Mentorship Management Platform",
  "scripts": {
    "postinstall": "npm install --prefix client && npm install --prefix server",
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server",
    "start": "node server/index.js",
    "dev": "npm run dev --prefix client"
  }
}
